import { AhvDirective } from './ahv.directive';

describe('AhvDirective', () => {
  it('should create an instance', () => {
    const directive = new AhvDirective();
    expect(directive).toBeTruthy();
  });
});
