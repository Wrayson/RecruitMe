import { CheDirective } from './che.directive';

describe('CheDirective', () => {
  it('should create an instance', () => {
    const directive = new CheDirective();
    expect(directive).toBeTruthy();
  });
});
